ktor {
    deployment {
        port = 8080,
    }
    application {
        modules = [ com.voidsamuraj.ApplicationKt.module ]
    }
}
jwt {
    domain = "https://jwt-provider-domain/"
    audience = "jwt-audience"
    realm = "ktor sample app"
    secret = "mojrzesz"
}
